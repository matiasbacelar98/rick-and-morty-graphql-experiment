export type CharacterType = {
  imgSrc: string;
  name: string;
  species: string;
  status: string;
  origin: string;
  id?: string;
};

export type CharacterStatusType = {
  id: string;
  value: string;
};
